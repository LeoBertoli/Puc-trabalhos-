int main()
{
    int valor = 0;
    printf("Por favor, digite um valor:\n")
    scanf("%d" &valor);
    if (valor > 0)
    {
        printf("contando até %d\n",valor);
        for (int i = 0; i <valor; i++)
        printf("%d\n",i);
    }
    else printf ("Valor menor do que 1\n");
    return 0;
}
